export const handleSearchChange = (setSearch) => (e) => {
    setSearch(e.target.value);
  };
  
  export const handleCategoryChange = (setCategory) => (e) => {
    setCategory(e.target.value);
  };
  